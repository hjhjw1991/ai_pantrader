# 候潮 默认策略。
#
# 这份文件是参数的**唯一真相源**（决策 D7）。参数面板只是它的投影：
# 面板上改一个数字 = 改这里的一行，不存在第二份状态、不存在数据库里的副本。
# 高级规则（因子之间的关系判断）直接手写在这里，不必等面板支持。
#
# 每个阈值下面的注释记的是"为什么是这个数"。改数字前先读它 ——
# 这些注释是几次真金白银的复盘留下来的，写回参数时 loader 会原样保留它们。

id: default
# 显示名，给人看的。界面上显示它，不显示 id。
# 随便改，不影响任何历史 —— 台账按 id 归因，不按这个名字。
名称: 候潮·默认
# 语义化版本。改了任何阈值都该升 patch，改了规则结构升 minor ——
# 否则历史回测结论无法归因到具体的哪一版参数（spec §9.2）。
version: 1.3.0

择时:
  # 三档只有三个数字，但语义差别是根本性的：
  # 防守 = 0，**不是"轻仓"**。留一成仓位过冬是最常见的自我安慰，
  # 结果是纪律形同虚设：既然还有仓位，就还会盯、还会补、还会扛。
  仓位档位:  { 进攻: 0.7, 中性: 0.4, 防守: 0.0 }
  # 任一条件成立即进防守。键名后缀 ">" 表示"因子值大于该阈值时触发"。
  # 权重杀跌 当前**没有对应因子**（需要指数成分股口径），引擎会把它列进
  # 信号卡 warnings 的"未判定条件"，而不是当成 false 悄悄放过 ——
  # 缺数据的条件静默通过，等于宣称"防守条件全查过了"，那是最危险的假阳性。
  防守触发:  { 跌停家数>: 30, 权重杀跌: true, 外围risk_off: true }

选股:
  # 七道筛的阈值。位置涨幅上限 50 = 近 20 日已涨 50% 以上不再新开仓：
  # 用户的目标是牛市长期稳定盈利，不打板不超短，追高段的胜率与他的执行方式不匹配。
  过滤器阈值: { 位置涨幅上限: 50, 换手上限: 15, 振幅上限: 10,
               超买否决分: 3, 解禁否决比例: 0.15, 减持否决比例: 0.03,
               估值否决分位: 0.95, 估值否决倍数: 2 }
  # 1.2.0 起在原七道之上加了三道风险否决：ST / 解禁减持 / 超买。
  #
  # 超买否决分 3 = "严重超买"（RSI / 乖离 / KDJ 三项投票，满分 4）。
  # 这道筛与"不限制打板"**天然有张力**：连板股的 RSI 几乎一定在 90 以上。
  # 默认只挡抛物线式急拉（横盘后连拉五板这种），稳步趋势只拿 2 分、不会被挡。
  # 想完全关掉就设成 99。累计涨幅过大是第一道"位置"筛的活，与这里不重复。
  #
  # 解禁否决比例 0.15 = 未来 90 天解禁占**流通股** 15% 以上才否决；5%~15% 只上卡片提示。
  # 减持否决比例 0.03 = 未来 90 天减持计划上限占**总股本** 3% 以上才否决。
  # 两个分母不同（流通 vs 总股本），所以分开判、不相加。
  #
  # 以上三个是**买入侧**的否决线。持仓侧"该不该抛"用的是另一组阈值（因子自己的
  # 严重线：解禁 15% / 减持 3% / 超买 3 分），在 因子参数.解禁压力.严重比例 这类键里调。
  # 两组分开是有意的：拿新钱买进和处置已有仓位，风险容忍本就不同。
  #
  # 估值否决分位 0.95 + 估值否决倍数 2（1.3.0 起）= PE 处于**申万一级行业内**最贵的 5%
  # **并且**是同行中位 PE 的 2 倍以上才否决。只看分位会误伤：工商银行 PE 8.27 在银行业
  # 排 0.95 分位，却只是同行中位的 1.6 倍 —— 那是大行溢价，不是估值畸高。
  # 按行业比而不是全市场：同样 PE 30，在半导体里偏便宜，在银行里是最贵的那一撮。
  # 只做粗筛 —— 资金跑出来的龙头本就不看便宜，这里只挡纯概念炒到同行几倍 PE 的票。
  # 亏损（PE 为负）默认不否决。估值只有 2026-09-23 起的当日快照，更早的日期这道筛是未判定。
  # 设成 1.01 等于关掉。
  # 必查链是**叠加**项，不是替换项：写空数组也照扫那四条链（spec §8.2 写死）。
  # 原因是 2026-07-27 的主线级漏扫 —— 板块涨幅榜给的是均值，
  # 一条链里几十只票拖后腿能把均值压到第 9 名，而链内已有 3 板龙头在封板。
  # 买点。触发价 = 昨收 × (1 + 相对昨收)；不高于MA5=true 时再与 MA5 取低。
  #
  # -1% 是**为了让台账攒出样本**而选的临时值，不是最优参数。
  #
  # 原先写死成 min(昨收, MA5)（"不追高，等回踩到位"）。方向没错，但和候选池的
  # 人口结构对不上：候选主要来自昨日涨停池，刚涨停的票 MA5 必然远在收盘价之下，
  # 次日通常还要高开，买点从生成那一刻起就在市价下方 5~10%。
  # 实测 4 年回测（964 个交易日）21 条买入决策**成交 0 笔**，触发率 0% ——
  # 台账因此永远是空的，胜率、盈亏比全都无从谈起。
  #
  # 放宽挂单有效期救不回来（同一批推荐：3 天 6%、5 天 6%、10 天 25%，
  # 而等到第 7~12 天才回踩，当初"昨日涨停 + 主线"的理由早就不在了）。
  #
  # -1% 只有 21 条样本支撑（触发率 48%、盈亏比 2.28），远低于统计门槛 30 条，
  # 是噪声不是结论。等台账攒够了按台账页的三关数据回来重定，
  # 改的时候记得升 version —— 预测按版本分组统计，不升就把两代样本混进一个分母。
  买点: { 相对昨收: -0.01, 不高于MA5: false }
  主线识别:  { 板块涨幅榜TopN: 3, 必查链: [半导体全链, 军工, 电网, 资源] }
  # 候选从哪几路来。三路都要过同一道主线筛与七道筛，只是"进池"的理由不同。
  #   涨停池   昨日涨停 —— 强度最直接的证据
  #   主线领涨 主线板块涨幅榜上的那只领涨股
  #   量价     主线板块成分里放量突破 + 均线多头排列的
  # 只留涨停池的话，入场手法是回踩低吸、标的池却全是打板股：
  # 主线里没涨停但形态更好的票永远进不来。
  候选来源: { 涨停池: true, 主线领涨: true, 量价: true }
  # 量价那一路的阈值。两条同时满足才算：
  # 放量抓"资金刚进来"，多头排列排掉"放量但趋势已坏"的出货。
  # 上市不足 20 个交易日的新股天然不满足（MA20 算不出来），这是有意的。
  量价条件: { 均量窗口: 5, 放量倍数: 1.5, 新高窗口: 20, 多头排列: true }

# 每账户一段规则，账户名**由你自己定**（键名随便改，加几个都行）。
# 这里的两个只是示例（一个吃波动、一个扛逻辑）；改名或删掉都不会影响代码运行。
#
# 生效前提：账户要在设置页的 account 表里存在（键名对应账户 id）。
# YAML 里配了、account 表里没有的账户，持仓页会明确提示"该账户硬线告警不生效"，
# 不会静默失效。
#
# 键名写 `卫星` 或 `卫星账户` 都认（结尾的"账户"二字可省），
# 但同一账户别写两种写法 —— 那会变成两份规则，实际生效哪份取决于遍历顺序。
持仓:
  # 不同账户的离场纪律可以完全不同，止损规则不可混用。
  # 这一套吃波动：止损是价格纪律，机械执行。
  # 可交易板块：这个账户开通了哪些板的权限。引擎按它把标的分派给账户，
  #             命中多个账户时取本文件里靠前的那个（顺序即优先级）。
  #             没有任何账户能交易的板块，不出信号 —— 出了也买不进。
  # 仓位桶：    该账户吃"核心"还是"卫星"预算（见下方 核心卫星比例）。不填则预算为 0 并在卡片上点名。
  卫星账户:  { 可交易板块: [主板], 仓位桶: 卫星,
              止损: -0.05, 灾难位: -0.08, 止损确认: 收盘, 止盈: [0.08减半, 0.15清] }
  # 止损确认: 收盘 —— 盘中破位不立刻砍。
  # 政策底/外围硬驱动的反弹日，盘中单次冲高回落多数是洗盘不是见光死。
  # 灾难位 -0.08 是例外：跌破它不等收盘，直接走。这就是"灾难位"存在的意义。
  #
  # 这一套扛逻辑：止损条件是"逻辑破坏"而不是一个百分比，
  # 所以它没有机械止损价，引擎不会给这类持仓填 stopPx，只会提示复核逻辑。
  核心账户:  { 可交易板块: [主板, 创业板, 科创板], 仓位桶: 核心,
              止损: 逻辑破坏, 加仓: 逆势分批 }

组合风控:
  # 上限而非目标。实际总仓位 = min(总仓位上限, 档位仓位)。
  总仓位上限: 0.8
  # 单票 15%：一只票踩到灾难位 -8%，对总资产的伤害约 1.2%，一天能消化。
  单票最大占比: 0.15
  # 单行业 35%：持仓集中在同一条产业链时，同源票一起跌，"分散"是假的。
  # 这条上限是对"看着是 5 只票、其实是 1 个赌注"的硬约束。
  单行业最大占比: 0.35
  # 核心=真业绩合理估值的长期仓，卫星=波动仓。具体对应哪个账户由你的账户划分决定。
  核心卫星比例: { 核心: 0.6, 卫星: 0.4 }
