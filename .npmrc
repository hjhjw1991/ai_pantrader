# Node 版本上下都卡死（package.json 的 engines + .nvmrc）。
#
# 默认 pnpm 只对版本不符 **打一行 WARN** 就照跑，于是 `pnpm dev` 在 Node 24 下能起来，
# 直到某个请求真去开库才炸成 503「数据库打不开」—— 那时报错离原因已经隔了好几层。
# engine-strict 把那行 WARN 变成启动即失败，任何 pnpm 入口都拦得住。
#
# 卡上界的理由见 README：better-sqlite3 的预编译 .node 绑 Node ABI，
# 而 install-launchd / install-schtasks 写进计划任务的是安装当时那个 Node 的绝对路径。
engine-strict=true
